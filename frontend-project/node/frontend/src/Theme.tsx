import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            light: "#ffc786",
            main: "#FF981F",
            dark: "#ca7617",
            contrastText: "#FFF2C1",
        }
    }
});

export default theme;
